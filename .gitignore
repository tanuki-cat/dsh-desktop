target/
.cargo-home/
.pnpm-store/
node_modules/

# staged bundled runtime: keep the marker, ignore the payload
src-tauri/runtime/*
!src-tauri/runtime/README.md

# downloaded node tarball + unpacked staging area
.runtime-cache/

# python bytecode from the staging helper scripts
__pycache__/

# gh CLI cache when XDG_CACHE_HOME is redirected into the repo
.gh-cache/
src-tauri/gen/

# icon artwork scratch space
.icon-work/

# local release artifacts (the CI writes its zips to the same directory)
dist/
