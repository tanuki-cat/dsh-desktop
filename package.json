{
  "name": "dsh-desktop",
  "private": true,
  "version": "0.4.6",
  "description": "Tauri shell that supervises dsh web and hosts it in the system WebView",
  "packageManager": "pnpm@12.3.4",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "cargo:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "cargo:test": "cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
